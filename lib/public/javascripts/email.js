const nodemailer=require("nodemailer"),config=require("../../../config.json");function sendMail(tos,msg){var from=config.email.from,subject=config.email.subject;const smtpTransport=nodemailer.createTransport({host:config.email.host,secure:!0,port:config.email.port,auth:{user:from,pass:config.email.password}});smtpTransport.sendMail({from:"前端自动化部署平台 <"+from+">",to:tos,subject:subject,html:msg},function(err,res){err&&console.log("error: ",err)})}module.exports={sendMail:sendMail};